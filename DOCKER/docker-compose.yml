version: '2'

services:
	OpenLiberty:
		build: ../images/openliberty
		links:
			-mysql:mysql
		ports:
			- "9443:9000"
	mysql:
		build: ../images/mysql
		environment:
			- MYSQL_ROOT_PASSWORD=root
		ports:
			- "3306:3306"
		volumes:
			- ../volumes/_data:/var/lib/mysql
